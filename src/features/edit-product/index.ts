// import { editProduct } from '@/entities/product/model/product-slice';
// import { AppDispatch } from '@/app/store';
// import { Product } from '@/shared/types/product';
// import EditProductModal from './ui/edit-product-modal'; // Изменяем на default импорт

// export const handleEditProduct = (product: Product, userId: string) => (dispatch: AppDispatch) =>
//   dispatch(editProduct(product, userId));

// export default EditProductModal; // Экспортируем как default

import ProductFormModal from '../product-form/ui/product-form-modal';

export default ProductFormModal;
